[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "ftnl-client"
version = "0.1.0"
description = "File Tunnel API client"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["ftnl_client"]
