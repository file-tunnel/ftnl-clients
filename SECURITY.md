# Security policy

Report vulnerabilities through GitHub Security Advisories. Never paste a live
pairing URI, bearer capability, event ticket, filename, or uploaded file into a
public issue.

SDK diagnostics must redact `Authorization`, URL fragments, ticket query
values, and response bodies that may contain capability material.
